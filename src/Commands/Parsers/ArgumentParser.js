import stringArgv from 'string-argv';
import { isNumber, toInteger, toNumber } from 'lodash/lang';

/**
 * Handles parsing of commands given by users.
 */
export default class ArgumentParser {
  static parse(rules, message) {

  }
}
